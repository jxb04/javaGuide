package chapter14;

// A generic functional interface with two parameters that returns a boolean result.
public interface SomeTest<T> {                // A generic functional interface
	boolean test(T n, T m);
}
